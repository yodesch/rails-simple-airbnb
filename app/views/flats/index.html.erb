<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <div class="container-fluid">
    Simple AirBnB


     <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <%# if user_signed_in? %>

          <%= link_to "Add Flat", new_flat_path, class: "btn btn-outline-primary" %>

          <li class="nav-item dropdown">
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <%= link_to "Action", "#", class: "dropdown-item" %>
              <%= link_to "Another action", "#", class: "dropdown-item" %>
              <%= link_to "Log out", "#", class: "dropdown-item", data: {turbo_method: :delete} %>
            </div>
          </li>
        <%# else %>
          <!--li class="nav-item"-->
            <%#= link_to "Login", new_user_session_path, class: "nav-link" %>
          <!--/li-->
        <%# end %>
      </ul>
      <div class="form">
        <form action="">
          <input class="form-control string required" type="text" name="search[query]" id="search_query" placeholder="Search"/>
          <%= link_to "Search", search_path, class: "btn btn-outline-success" %>
        </form>
      </div>
    </div>
  </div>
</div>







<div class="container">
  <h1>All our Flats</h1>



  <div class= "d-flex flex-wrap">
    <% @flats.each do |flat| %>
    <div class="card-trip">
      <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/greece.jpg" />
        <div class="card-trip-infos">
          <div>
            <h2><%= flat.name %></h2>
              <p><%= flat.description %></p>
              <p><i class="fa-solid fa-people-roof"></i><%= flat.number_of_guests%></p>
          </div>
            <h2 class="card-trip-pricing"><%= flat.price_per_night%>€</h2>
          </div>
          <div>
            <%= link_to "Remove",flat_path(flat), class:"btn btn-link text-danger",
                data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
            <%= link_to "Edit", edit_flat_path(flat), class:"btn btn-link text-warning",data: {turbo_method: :get} %>
          </div>
        </div>
    </div>
    <% end %>
  </div>
</div>
